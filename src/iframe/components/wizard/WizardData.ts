/**
 * Data collected across the wizard steps.
 * Only the fields used by built steps exist; future steps (timing, scope)
 * add their own fields here once implemented.
 */
export class WizardData {
  name = ''
  description = ''
}
