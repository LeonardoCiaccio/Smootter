/**
 * environment — injected on demand when the toolbar icon is clicked (chrome.scripting).
 * Never runs on its own: loaded only when needed.
 * For now: bare, does a single thing.
 */
console.log('hello world')
